1.  原生js的onload方法会等到dom及图片加载完才会执行
    而jquery$(document).ready()不会等到图片加载完就会执行
2.  原生js重复执行入口函数会覆盖之前的而jquery不会	

3.  事件委托， 利用事件冒泡原理，将子元素触发的事件交给父元素来处理，即给父员绑定相应的事件，这样子元素触发的事件，
               就会被父元素捕获，避免子元素重复添加事件，提高效率















